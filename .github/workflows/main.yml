on: push
name: 🚀 Deploy Olympic Clicker on push
jobs:
  web-deploy:
    name: 🎉 Deploy
    runs-on: ubuntu-latest
    steps:
    - name: 📂 Sync files
      uses: SamKirkland/FTP-Deploy-Action@v4.3.5
      with:
        server: 167.86.94.65
        username: alpha_main
        password: ${{ secrets.ftp_password }}
        server-dir: /OlympicClicker/